set(CLIENT_SRC_LIST
        ${CPT_API_SRC}
        ${TCP_CLIENT_SRC}
        ${APP_CLIENT_SRC}
        PARENT_SCOPE)


set(SERVER_SRC_LIST
        ${CPT_API_SRC}
        ${TCP_SERVER_SRC}
        ${APP_SERVER_SRC}
        PARENT_SCOPE)