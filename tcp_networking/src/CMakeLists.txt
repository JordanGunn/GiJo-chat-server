SET(TCP_CLIENT_SRC
        ${CPT_SOURCE_DIR}/tcp_networking/src/tcp_client.c
        PARENT_SCOPE
        )

SET(TCP_SERVER_SRC
        ${CPT_SOURCE_DIR}/tcp_networking/src/tcp_server.c
        PARENT_SCOPE
        )